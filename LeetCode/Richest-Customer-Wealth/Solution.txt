1class Solution {
2public:
3    int maximumWealth(vector<vector<int>>& s) {
4        int maxi = 0 ;
5        for(int i=0;i<s.size();i++){
6            int total = accumulate(s[i].begin(),s[i].end(),0);
7            maxi = max(total,maxi);
8        }
9        return maxi;
10    }
11};