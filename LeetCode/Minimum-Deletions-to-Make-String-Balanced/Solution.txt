1class Solution {
2public:
3    int minimumDeletions(string s) {
4        stack<int>st;
5        int count = 0;
6        for(int i=0;i<s.size();i++){
7            if(s[i] == 'a' && !st.empty() && st.top() == 'b' ){
8                st.pop();
9                count++;
10            }
11            else{
12                st.push(s[i]);
13            }
14        }
15        return count;
16    }
17};