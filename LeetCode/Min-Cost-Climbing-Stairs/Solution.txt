1class Solution {
2public:
3    int minCostClimbingStairs(vector<int>& cost) {
4
5        vector<int> t(cost.size() + 1);
6
7        t[0] = cost[0];
8        t[1] = cost[1];
9
10        for (int i = 2; i <= cost.size(); i++) {
11            if(i<cost.size())
12          {  t[i] = cost[i] + min(t[i - 1], t[i - 2]);}
13        }
14        return min(t[cost.size() - 1], t[cost.size() - 2]);
15    }
16};